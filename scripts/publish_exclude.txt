# publish_exclude.txt
#
# Paths removed from the rebranded tree BEFORE publishing to the downstream
# `rimon-fc/substrate` repo. These are the rebrand/publish machinery that must
# never appear downstream, so the published repo looks hand-crafted.
#
# Format: one path per line, relative to repo root. Lines starting with `#`
# and blank lines are ignored. A trailing `/` means "directory, recursive".
# Matching is exact-path (after rebrand renames are applied).
#
# NOTE: the entire .github/workflows/ tree is stripped (see below). The
# published snapshot ships no CI: it is a generated mirror, not a repo that
# runs upstream's tests/release/security jobs (which only fail or fire need-
# lessly downstream and break the hand-crafted illusion).

# --- rebrand tooling ---
scripts/rebrand.py

# --- fork->downstream pipeline scripts ---
scripts/sync_upstream.sh
scripts/publish_substrate.sh
scripts/publish_exclude.txt

# --- ALL workflows + CI helper scripts: the published repo runs no CI ---
.github/workflows/

# --- the overlay source-of-truth (README/art/examples live here in the fork,
#     and are layered onto the published tree; the overlay dir itself never
#     ships downstream) ---
substrate-overlay/
